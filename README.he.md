<div align="center">

# Claude Desktop RTL Patch

**תמיכה אמיתית בעברית ובערבית (כיווניות מימין לשמאל) ל-Claude Desktop ב-Windows.**

מזהה טקסט RTL אוטומטית ומיישר את הכיוון במקומות הנכונים, בלי לשבור אנגלית או בלוקי קוד.

[English](README.md) · [עברית](README.he.md)

[![License: MIT](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Platform: Windows](https://img.shields.io/badge/platform-Windows%2010%2F11-0078D6.svg)](#-דרישות)
[![PowerShell 5.1+](https://img.shields.io/badge/PowerShell-5.1%2B-5391FE.svg)](#-דרישות)
[![PRs welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg)](#-תרומה-לפרויקט)
[![GitHub stars](https://img.shields.io/github/stars/shraga100/claude-desktop-rtl-patch?style=social)](https://github.com/shraga100/claude-desktop-rtl-patch/stargazers)

</div>

---

<div dir="rtl">

> **📢 עדכון (יולי 2026): קלוד השיקה תמיכת RTL רשמית — והפאטצ' שודרג מהיסוד כדי להשתלב איתה.**
>
> אנתרופיק הוסיפה רינדור RTL נייטיבי ל**כרטיסיית Code**, והקוד עבור **כרטיסיית Chat** כבר קיים אצלם (כרגע כבוי — כנראה בדרך). אלו חדשות טובות באמת, ואני מקווה שבקרוב הפאטצ' יתייתר לגמרי.
>
> בינתיים, ללוגיקה הנייטיבית עדיין יש פערים אמיתיים: אין כיוון פר-תא בטבלאות, אין טיפול בשורות מעורבות-שפה, רשימה מעורבת נשפטת לפי הפריט הראשון בלבד, אין בידוד מתמטיקה/LaTeX, ואין כלום עבור הודעות המשתמש, תיבת הקלט ושאר הממשק. גם בעיית החלון השבור במערכות RTL עדיין לא נפתרה נייטיבית — כולל חלון התצוגה המקדימה שנדחף לקצה השמאלי.
>
> לכן הפאטצ' עבר שדרוג מקיף: הוא **מתמזג עם ה-RTL הנייטיבי במקום להילחם בו** — היכן שיש תמיכה נייטיבית הוא לא נוגע, ומנוע הזיהוי שלו משלים את כל מה שהנייטיב עדיין מפספס. כשאנתרופיק ידליקו את התמיכה בכרטיסיית Chat, הפאטצ' ייסוג שם אוטומטית — בלי צורך בעדכון.

## ✨ מה זה עושה

- **מזהה אוטומטית טקסט בעברית ובערבית** בתשובות של Claude ובתיבת הקלט, ומיישר את הכיוון בזמן אמת, גם בזמן שתשובה עדיין נכתבת.
- **מטפל בקוד, בנוסחאות מתמטיות ובטבלאות** — בלוקי קוד ונוסחאות LaTeX (`$x^2$`, `$$…$$`) נשארים משמאל לימין בתוך עברית, וטבלאות עבריות נקראות מימין לשמאל.
- **מתקן את ממשק החלון שנשבר במערכות בכיווניות RTL** (פירוט בהערה למטה).
- **מגבה כל קובץ שהוא משנה**, עם שחזור בלחיצה אחת וחזרה אוטומטית למצב הקודם אם משהו משתבש.
- **נשאר מותקן גם אחרי עדכונים** (אופציונלי), דרך קיצור דרך בשולחן העבודה או רכיב עדכון אוטומטי שרץ ברקע.

**📝 הערה:** כשמערכת ההפעלה Windows מוגדרת לעברית או לערבית, כל ממשק החלון של Claude Desktop מתהפך לכיווניות RTL מעצמו. זה באג קיים שלא נגרם מהפאצ': כפתורי פס הכותרת (מזעור, הגדלה, סגירה) נדחפים אל מעל כפתורי ההגדרות והניווט של Claude ומסתירים אותם, וחלון התצוגה המקדימה קופץ לקצה השמאלי. הפאצ' מתקן את זה בכך שהוא מחזיר את כיווניות מסגרת החלון ל-LTR, בלי לפגוע בכיוון של טקסט השיחה עצמו.

---

## 🚀 התקנה מהירה

פתחו את **Windows PowerShell** והריצו:

<div dir="ltr">

```powershell
irm https://raw.githubusercontent.com/shraga100/claude-desktop-rtl-patch/main/install.ps1 | iex
```

</div>

תופיע בקשת UAC. לחצו **Yes** כדי לאשר הרשאות מנהל, ואז בחרו באפשרות **1. Install** מהתפריט.

**💡 טיפ:** מעדיפים לא להריץ דרך `iex`? הורידו את `patch.ps1` מהמאגר, לחצו עליו קליק ימני ובחרו **Run with PowerShell**.

---

## 📋 דרישות

| דרישה | פירוט |
| :--- | :--- |
| **Windows 10 / 11** | עם Claude Desktop מותקן ([הורדה](https://downloads.claude.ai/releases/win32/ClaudeSetup.exe)) |
| **Node.js** | `npx` צריך להיות זמין ב-PATH ([nodejs.org](https://nodejs.org/)), משמש לאריזה מחדש של ארכיון האפליקציה |
| **הרשאות מנהל** | המתקין מבקש אותן אוטומטית דרך UAC |

**⚠️ חשוב:** Windows בלבד. ב-macOS השתמשו בפאצ' של [soguy](https://github.com/soguy/claude-desktop-rtl-mac) במקום. *(לא בדקתי את גרסת ה-Mac בעצמי, השימוש על אחריותכם.)*

---

## 🎛️ אפשרויות התפריט

כשמריצים את הסקריפט מתקבל תפריט אינטראקטיבי:

| # | אפשרות | מה היא עושה |
| :---: | :--- | :--- |
| **1** | Install Smart RTL Patch | מגבה את הקבצים המקוריים ומזריק את תמיכת ה-RTL |
| **2** | Restore Original State | מבטל את כל השינויים שהפאצ' ביצע |
| **3** | Create 'Quick Update' Shortcut | מוסיף קיצור דרך לשולחן העבודה להחלת הפאצ' מחדש בלחיצה |
| **4** | Enable Auto Re-Patch | מפעיל עדכון אוטומטי שמחיל את הפאצ' מחדש אחרי כל עדכון של Claude |
| **5** | Disable Auto Re-Patch | מכבה את העדכון האוטומטי |
| **6** | Exit | סוגר את המתקין |

---

## 🔄 לשמור על הפאצ' מותקן

מכיוון ש-Claude Desktop מתעדכן לעיתים קרובות, כל עדכון דורס את הפאצ'. שתי אפשרויות אופציונליות הופכות את התחזוקה לפשוטה:

- **קיצור Quick Update (אפשרות 3):** קיצור דרך בשם **Update Claude RTL**. לחיצה כפולה עליו מורידה ומחילה מחדש את הגרסה האחרונה, בלי להיכנס לתפריט.
- **עדכון אוטומטי (אפשרות 4):** משימה מתוזמנת קלילה של Windows. היא מזהה מתי עולה גרסה חדשה של `claude.exe`, מחילה את הפאצ' מחדש אוטומטית, ומציגה התראה בסיום.

---

## 🗑️ הסרה

הריצו את הסקריפט ובחרו באפשרות **2. Restore Original State**. הפעולה משחזרת את כל הקבצים המקוריים מהגיבוי ומסירה את התעודה החתומה-עצמית ממאגר התעודות של Windows. אם הפעלתם את העדכון האוטומטי, בחרו גם באפשרות **5. Disable Auto Re-Patch**.

---

## 🛠️ פתרון תקלות

**מופיע `Node.js (npx) is required`:** התקינו Node.js מ-[nodejs.org](https://nodejs.org/) ופתחו מחדש את PowerShell.

**אם Claude לא עולה אחרי ההחלה:** הריצו שוב את הסקריפט, בחרו **2 (Restore)** ואז **1 (Install)**.

**אם Claude התעדכן והפאצ' הפסיק לעבוד:** הריצו את קיצור **Update Claude RTL**, או השתמשו בעדכון האוטומטי. לתיקון ידני, מחקו את קבצי ה-`.bak` מתיקיית האפליקציה של Claude והריצו שוב את המתקין.

**מופיעה השגיאה `Import-Module … The member AuditToString is already present`:** הרצתם את הפקודה ב-**PowerShell 7 (`pwsh`)**, שיש בו באג ידוע ששובר את הפאצ'. השתמשו במקום זאת ב-**Windows PowerShell** המובנה:

1. הקישו **Win + R**, הקלידו `powershell` והקישו **Enter**. זה פותח את חלון Windows PowerShell ה**כחול** הקלאסי (לא את ה-`pwsh` השחור).
2. הדביקו את פקודת ההתקנה והריצו אותה.

---

<details>
<summary><b>🔍 איך זה עובד (טכני)</b></summary>

<div dir="rtl">

מכיוון ש-Claude Desktop היא אפליקציית Electron **חתומה דיגיטלית**, עריכה של קוד הממשק שלה שוברת את בדיקות השלמות. הפאצ' עוקף את זה בשלושה שלבים, כולם אטומיים ועם חזרה אוטומטית למצב הקודם בכל כשל:

**שלב 1, הזרקת ASAR.** הממשק של Claude נמצא בתוך `app.asar`. הסקריפט מחלץ אותו עם `npx @electron/asar`, מוסיף לקבצי ה-renderer את ה-payload שמזהה RTL (וכופה את ממשק החלון ל-LTR בתהליך הראשי), אורז מחדש את הארכיון, ומחשב לכותרת שלו hash חדש של SHA-256. לוגיקת הזיהוי של ה-payload נמצאת ב-[`src/rtl-core.js`](src/rtl-core.js) (טהורה ונבדקת ביחידה) ו-[`src/rtl-payload.js`](src/rtl-payload.js) (שכבת ה-DOM); הסקריפט `tools/build-payload.ps1` מרכיב אותם לתוך `patch.ps1`.

**שלב 2, החלפת ה-hash בתוך `claude.exe`.** ה-hash המקורי של ה-ASAR שמור כמחרוזת ASCII בתוך `claude.exe`. הסקריפט מבצע חיפוש-והחלפה ברמת הבייט כדי לעדכן אותו. אם הפורמט ישתנה אי-פעם, יש מנגנון גיבוי שמכבה את ה-Electron integrity fuse.

**שלב 3, החלפת התעודה בתוך `cowork-svc.exe`.** השירות הזה מאמת את `claude.exe` מול התעודה המוטמעת של Anthropic. הסקריפט מאתר אותה (לפי התאמת תבנית סביב המחרוזת `"Anthropic, PBC"`), מחליף אותה במקום בתעודה חתומה-עצמית (מרופדת ב-`0x00` כדי לשמר את גודל הקובץ), חותם מחדש את שני הקבצים, מוסיף את התעודה החדשה למאגר ה-trusted root של Windows, ואז מוחק את המפתח הפרטי.

כל הקבצים המקוריים מגובים כקבצי `.bak` ליד המקור לפני שמשהו משתנה.

</div>

</details>

<details>
<summary><b>🔐 אבטחה ואימות</b></summary>

<div dir="rtl">

הקובץ `install.ps1` מאמת חתימת **RSA-4096** על `patch.ps1` לפני שהוא מריץ אותו. מאגר GitHub פרוץ לבדו **אינו** מספיק כדי להפיץ קוד זדוני; התוקף היה צריך גם את המפתח הפרטי הלא-מקוון של המתחזק.

**טביעת אצבע של המפתח הציבורי (SHA-256):**

<div dir="ltr">

```
6e:f4:c2:a6:c2:42:34:a1:5f:e5:cd:e5:5d:a5:b0:3c:94:64:b4:56:7f:81:04:7c:83:9a:50:1c:7c:6f:07:c9
```

</div>

כדי לוודא, חשבו מחדש את טביעת האצבע של המפתח המוטמע ב-`install.ps1` ובדקו שהיא תואמת:

<div dir="ltr">

```powershell
$content = Invoke-RestMethod "https://raw.githubusercontent.com/shraga100/claude-desktop-rtl-patch/main/install.ps1"
if ($content -match "ExpectedPubKey\s*=\s*'([A-Za-z0-9+/=]+)'") {
    $bytes = [Convert]::FromBase64String($matches[1])
    $hash  = [System.Security.Cryptography.SHA256]::Create().ComputeHash($bytes)
    ([BitConverter]::ToString($hash)).Replace('-', ':').ToLower()
}
```

</div>

אם יש אי-התאמה, המשמעות היא שהמפתח המוטמע הוחלף. אל תמשיכו, ודווחו על כך כבעיית אבטחה.

לבדיקה של גרסה בלי להתקין:

<div dir="ltr">

```powershell
git clone https://github.com/shraga100/claude-desktop-rtl-patch
cd claude-desktop-rtl-patch
powershell -ExecutionPolicy Bypass -File tools\verify-signature.ps1
```

</div>

</div>

</details>

---

## ⚠️ כתב ויתור

**🛑 זהירות:** הפאצ' הזה משנה את קבצי הבינארי של Claude Desktop. הוא מחליף את תעודת חתימת-הקוד של Anthropic בתוך `cowork-svc.exe` בתעודה חתומה-עצמית, מוסיף את התעודה הזו למאגר ה-trusted root של Windows, ועוקף את אימות השלמות של האפליקציה.

בהתקנה אתם מסכימים לתנאים הבאים:

1. **השימוש על אחריותכם בלבד.** הכותבים אינם אחראים לנזק למערכת, לאובדן מידע או לחוסר יציבות.
2. **ייתכן ששינוי האפליקציה אינו עולה בקנה אחד עם תנאי השימוש של Anthropic.** כדאי להכיר את התנאים ולהשתמש בשיקול דעת.
3. **אתם סומכים על המאגר הזה.** הרצת `irm | iex` כמנהל מריצה קוד עם הרשאות מלאות, אז תמיד כדאי לאמת את המקור.
4. **זה זמני.** עדכוני Claude דורסים את הקבצים המתוקנים, אז צריך להריץ שוב את המתקין (או להשתמש בעדכון האוטומטי) אחרי כל עדכון.
5. **זה פתרון ביניים** עד ש-Anthropic תוסיף תמיכת RTL מובנית. אנא בקשו את הפיצ'ר הזה דרך הערוצים הרשמיים.

---

## 🤝 תרומה לפרויקט

הפרויקט הוא קוד פתוח תחת רישיון **MIT**. תרומות שמשפרות את דיוק ה-RTL יתקבלו בברכה, וה-PRs פתוחים. 🙏

הקרדיט על גישת בידוד הנוסחאות המתמטיות שמור ל-[Claude-UniMath](https://github.com/DavidiBellaire/Claude-UniMath) מאת Davidi Bellaire.

## רישיון

[MIT](LICENSE)

</div>
